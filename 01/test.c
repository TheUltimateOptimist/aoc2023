#include <stdio.h>
#include <string.h>

void replace(char * text, char * new) {
    printf("%lu", sizeof(text));
    printf("%lu", strlen(text));
    str
    printf("%lu", sizeof(new));
    printf("%s", new);
}

int main() {
    char text[5] = "hallo";
    replace("hallo", "some");
}